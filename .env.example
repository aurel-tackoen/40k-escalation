# Auth0 Configuration
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_CALLBACK_URL=http://localhost:3000/api/auth/callback
AUTH0_LOGOUT_URL=http://localhost:3000

# Session Secret (generate with: openssl rand -base64 32)
SESSION_SECRET=your-random-secret-key-here

# Database Configuration
# For local development with Neon branch:
# 1. Create a branch in Neon Console (e.g., 'local' or 'dev')
# 2. Copy the connection string from the branch
# 3. Set it here as DATABASE_URL
# For production: Netlify automatically sets NETLIFY_DATABASE_URL
DATABASE_URL=postgresql://user:password@ep-xxx.region.neon.tech/dbname?sslmode=require

# Netlify automatically provides this in production
# NETLIFY_DATABASE_URL is set by Netlify's Neon integration