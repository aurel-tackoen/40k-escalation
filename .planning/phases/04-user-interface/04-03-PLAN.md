---
phase: 04-user-interface
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/components/admin/LeaguesManager.vue
  - app/components/admin/MatchesManager.vue
autonomous: true

must_haves:
  truths:
    - "Admin UI shows 'Phase' in user-visible labels"
    - "Admin edit modals use phase terminology"
    - "Admin tables display phase information correctly"
  artifacts:
    - path: "app/components/admin/LeaguesManager.vue"
      provides: "Admin league management with phase labels"
      contains: "Phase"
    - path: "app/components/admin/MatchesManager.vue"
      provides: "Admin match management with phase labels"
      contains: "Phase"
  key_links:
    - from: "app/components/admin/LeaguesManager.vue"
      to: "admin rounds modal"
      via: "round edit functionality"
      pattern: "Phase {{ round.number }}"
---

<objective>
Update admin components to display "Phase" terminology in user-visible labels and forms.

Purpose: Admin users should see consistent "Phase" terminology when managing leagues and matches. This ensures the entire application uses unified terminology.

Output: Admin interfaces display "Phase" instead of "Round" in all user-visible text.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-interface/04-01-SUMMARY.md
@.planning/phases/04-user-interface/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LeaguesManager admin component</name>
  <files>app/components/admin/LeaguesManager.vue</files>
  <action>
Update user-visible text in the admin leagues manager. Internal variable names can stay as-is.

**Edit form field label (line 757):**
- "Current Round" -> "Current Phase"

**Progress display (line 519):**
- "Round" text before the number -> "Phase"
- `Round <span...>{{ league.currentRound }}</span>` -> `Phase <span...>{{ league.currentPhase }}</span>`

**Show Rounds button (lines 615-627):**
- Button text: "Show Rounds" / "Hide Rounds" -> "Show Phases" / "Hide Phases"
- Count text: keep `league.rounds?.length` as data reference

**Expandable rounds section header (implicit in button):**
- The rounds are displayed with "Round {{ round.number }}" - change to "Phase {{ round.number }}"

**Round item display (line 645):**
- "Round {{ round.number }}: {{ round.name }}" -> "Phase {{ round.number }}: {{ round.name }}"

**Round Edit Modal title (line 807):**
- "Edit Round" -> "Edit Phase"

**Round Edit Modal labels:**
- "Round Number" (line 813) -> "Phase Number"
- "Round Name" (line 836) -> "Phase Name"

**Round Edit Modal button (line 875):**
- "Update Round" -> "Update Phase"

NOTE: Keep internal variable names (roundForm, editingRound, showRoundModal, etc.) unchanged - only update user-visible strings.
  </action>
  <verify>
Search file for user-visible "Round" text in template strings - should find none.
Run `npm run build` to verify no errors.
  </verify>
  <done>
LeaguesManager shows "Phase" in all labels, modal titles, and buttons. Admin can manage phases with consistent terminology.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MatchesManager admin component</name>
  <files>app/components/admin/MatchesManager.vue</files>
  <action>
Update user-visible text in the admin matches manager.

**League & Round Info section (line 516):**
- "Round" label -> "Phase" label
- Keep `{{ match.round }}` as data reference (API returns round field)

**Edit Modal Round field label (line 584):**
- "Round" -> "Phase"

NOTE: The match data structure uses `round` field from the database, so keep `match.round` and `editForm.round` as data references. Only change the visible label text.

The MatchesManager interacts with the matches table which still uses `round` column (matches record which round/phase a game was played in). The property access stays as `.round` but the UI label says "Phase".
  </action>
  <verify>
Search file for user-visible "Round" label text - should find none.
Run `npm run build` to verify no errors.
  </verify>
  <done>
MatchesManager shows "Phase" in all labels while correctly accessing match.round data property.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` passes without errors
2. Visual inspection (if admin access available):
   - /admin -> Leagues Manager shows "Current Phase", "Show Phases" button, "Phase N" in round/phase list
   - /admin -> Matches Manager shows "Phase" label in info section and edit modal
3. Grep verification: `grep -n "Round" app/components/admin/LeaguesManager.vue app/components/admin/MatchesManager.vue`
   - Should only find: variable names (roundForm, editingRound), not UI label strings
</verification>

<success_criteria>
1. LeaguesManager displays "Phase" in progress indicator
2. LeaguesManager displays "Phase" in expandable section
3. LeaguesManager edit modal uses "Phase" labels
4. MatchesManager displays "Phase" in info section
5. MatchesManager edit modal uses "Phase" label
6. Application compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-interface/04-03-SUMMARY.md`
</output>
